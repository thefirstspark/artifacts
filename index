<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The First Spark | Interactive Tools</title>
    <meta name="description" content="Consciousness navigation tools and interactive experiences from The First Spark">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600&family=Cormorant+Garamond:wght@400;500&family=Space+Mono&display=swap" rel="stylesheet">
    <style>
        :root {
            --void: #0a0a0f;
            --deep: #12121a;
            --gold: #d4af37;
            --ember: #ff6b35;
            --white: #f5f5f0;
            --ghost: rgba(245, 245, 240, 0.7);
            --whisper: rgba(245, 245, 240, 0.25);
            --glow: rgba(212, 175, 55, 0.3);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Cormorant Garamond', serif;
            background: var(--void);
            color: var(--white);
            min-height: 100vh;
            line-height: 1.6;
        }

        /* Animated Background */
        .bg-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            pointer-events: none;
        }

        /* Main Container */
        .container {
            position: relative;
            z-index: 10;
            max-width: 1200px;
            margin: 0 auto;
            padding: 60px 30px;
        }

        /* Header */
        .header {
            text-align: center;
            margin-bottom: 80px;
        }

        .logo-sigil {
            width: 120px;
            height: 120px;
            margin-bottom: 30px;
            animation: pulse-glow 4s ease-in-out infinite;
            filter: drop-shadow(0 0 20px var(--glow));
        }

        @keyframes pulse-glow {
            0%, 100% { filter: drop-shadow(0 0 20px var(--glow)); }
            50% { filter: drop-shadow(0 0 40px var(--gold)); }
        }

        .site-title {
            font-family: 'Cinzel', serif;
            font-size: clamp(2rem, 6vw, 3.5rem);
            font-weight: 400;
            letter-spacing: 0.25em;
            margin-bottom: 15px;
            background: linear-gradient(135deg, var(--white) 0%, var(--gold) 50%, var(--ember) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .site-subtitle {
            font-family: 'Space Mono', monospace;
            font-size: 11px;
            letter-spacing: 5px;
            color: var(--ghost);
            text-transform: uppercase;
        }

        /* Tools Grid */
        .tools-section {
            margin-bottom: 60px;
        }

        .section-title {
            font-family: 'Cinzel', serif;
            font-size: 14px;
            letter-spacing: 4px;
            color: var(--gold);
            text-transform: uppercase;
            margin-bottom: 30px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--whisper);
        }

        .tools-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 25px;
        }

        /* Tool Card */
        .tool-card {
            background: linear-gradient(145deg, rgba(26, 26, 46, 0.7) 0%, rgba(18, 18, 26, 0.9) 100%);
            border: 1px solid var(--whisper);
            padding: 35px;
            text-decoration: none;
            color: inherit;
            position: relative;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
            display: block;
        }

        .tool-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, var(--gold), var(--ember), var(--gold));
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.5s ease;
        }

        .tool-card:hover::before {
            transform: scaleX(1);
        }

        .tool-card:hover {
            transform: translateY(-5px);
            border-color: var(--gold);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4), 0 0 40px var(--glow);
        }

        .tool-icon {
            width: 50px;
            height: 50px;
            margin-bottom: 20px;
        }

        .tool-title {
            font-family: 'Cinzel', serif;
            font-size: 18px;
            letter-spacing: 3px;
            color: var(--gold);
            margin-bottom: 12px;
            text-transform: uppercase;
        }

        .tool-description {
            font-size: 16px;
            color: var(--ghost);
            line-height: 1.7;
            margin-bottom: 20px;
        }

        .tool-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .tool-tag {
            font-family: 'Space Mono', monospace;
            font-size: 9px;
            letter-spacing: 2px;
            color: var(--ember);
            text-transform: uppercase;
            padding: 5px 12px;
            border: 1px solid var(--ember);
            opacity: 0.8;
        }

        .tool-arrow {
            font-family: 'Space Mono', monospace;
            font-size: 12px;
            color: var(--gold);
            opacity: 0;
            transform: translateX(-10px);
            transition: all 0.3s ease;
        }

        .tool-card:hover .tool-arrow {
            opacity: 1;
            transform: translateX(0);
        }

        /* Coming Soon Card */
        .tool-card.coming-soon {
            opacity: 0.6;
            pointer-events: none;
        }

        .tool-card.coming-soon .tool-tag {
            color: var(--whisper);
            border-color: var(--whisper);
        }

        /* Membership CTA */
        .cta-section {
            text-align: center;
            padding: 60px 40px;
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.05) 0%, rgba(26, 26, 46, 0.5) 100%);
            border: 1px solid var(--whisper);
            margin-top: 60px;
        }

        .cta-title {
            font-family: 'Cinzel', serif;
            font-size: 24px;
            letter-spacing: 4px;
            color: var(--white);
            margin-bottom: 15px;
        }

        .cta-text {
            font-size: 18px;
            color: var(--ghost);
            max-width: 500px;
            margin: 0 auto 30px;
            line-height: 1.8;
        }

        .cta-button {
            display: inline-block;
            font-family: 'Cinzel', serif;
            font-size: 13px;
            letter-spacing: 3px;
            text-transform: uppercase;
            padding: 18px 50px;
            background: transparent;
            border: 1px solid var(--gold);
            color: var(--gold);
            text-decoration: none;
            transition: all 0.4s ease;
        }

        .cta-button:hover {
            background: var(--gold);
            color: var(--void);
            box-shadow: 0 0 40px var(--glow);
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 50px 20px;
            margin-top: 60px;
            border-top: 1px solid var(--whisper);
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-bottom: 25px;
            flex-wrap: wrap;
        }

        .footer-link {
            font-family: 'Space Mono', monospace;
            font-size: 10px;
            letter-spacing: 2px;
            color: var(--ghost);
            text-decoration: none;
            text-transform: uppercase;
            transition: color 0.3s ease;
        }

        .footer-link:hover {
            color: var(--gold);
        }

        .footer-copy {
            font-family: 'Space Mono', monospace;
            font-size: 10px;
            letter-spacing: 2px;
            color: var(--whisper);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .container {
                padding: 40px 20px;
            }

            .tools-grid {
                grid-template-columns: 1fr;
            }

            .header {
                margin-bottom: 50px;
            }

            .cta-section {
                padding: 40px 25px;
            }
        }
    </style>
</head>
<body>
    <canvas class="bg-canvas" id="bgCanvas"></canvas>

    <div class="container">
        <!-- Header -->
        <header class="header">
            <svg class="logo-sigil" viewBox="0 0 200 200">
                <defs>
                    <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                        <stop offset="0%" style="stop-color:#d4af37"/>
                        <stop offset="50%" style="stop-color:#ff6b35"/>
                        <stop offset="100%" style="stop-color:#d4af37"/>
                    </linearGradient>
                </defs>
                <circle cx="100" cy="100" r="90" fill="none" stroke="url(#goldGradient)" stroke-width="1"/>
                <circle cx="100" cy="100" r="70" fill="none" stroke="url(#goldGradient)" stroke-width="0.5"/>
                <polygon points="100,20 180,160 20,160" fill="none" stroke="url(#goldGradient)" stroke-width="1"/>
                <polygon points="100,180 20,40 180,40" fill="none" stroke="url(#goldGradient)" stroke-width="1"/>
                <circle cx="100" cy="100" r="20" fill="url(#goldGradient)" opacity="0.8"/>
            </svg>
            <h1 class="site-title">THE FIRST SPARK</h1>
            <p class="site-subtitle">Interactive Consciousness Tools</p>
        </header>

        <!-- Featured Tools -->
        <section class="tools-section">
            <h2 class="section-title">Featured Experiences</h2>
            <div class="tools-grid">
                <!-- Sparkverse -->
                <a href="sparkverse-tool.html" class="tool-card">
                    <svg class="tool-icon" viewBox="0 0 50 50">
                        <circle cx="25" cy="25" r="20" fill="none" stroke="#d4af37" stroke-width="1"/>
                        <circle cx="25" cy="25" r="12" fill="none" stroke="#d4af37" stroke-width="0.5"/>
                        <polygon points="25,8 42,38 8,38" fill="none" stroke="#d4af37" stroke-width="1"/>
                        <polygon points="25,42 8,12 42,12" fill="none" stroke="#d4af37" stroke-width="1"/>
                        <circle cx="25" cy="25" r="5" fill="#ff6b35"/>
                    </svg>
                    <h3 class="tool-title">The Sparkverse</h3>
                    <p class="tool-description">Complete consciousness navigation system with 9 interactive modules: Soul Map, Spark Tracker, Sigil Forge, Frequency Tuner, Ritual Builder, and more.</p>
                    <div class="tool-meta">
                        <span class="tool-tag">Premium</span>
                        <span class="tool-arrow">Enter →</span>
                    </div>
                </a>

                <!-- Add more tools here as you create them -->
                <a href="#" class="tool-card coming-soon">
                    <svg class="tool-icon" viewBox="0 0 50 50">
                        <rect x="5" y="10" width="40" height="30" fill="none" stroke="#d4af37" stroke-width="1"/>
                        <line x1="5" y1="20" x2="45" y2="20" stroke="#d4af37" stroke-width="0.5"/>
                        <circle cx="35" cy="30" r="8" fill="none" stroke="#ff6b35" stroke-width="1"/>
                    </svg>
                    <h3 class="tool-title">Soul Mapping Quiz</h3>
                    <p class="tool-description">Discover your unique consciousness blueprint through an interactive assessment journey.</p>
                    <div class="tool-meta">
                        <span class="tool-tag">Coming Soon</span>
                        <span class="tool-arrow"></span>
                    </div>
                </a>

                <a href="#" class="tool-card coming-soon">
                    <svg class="tool-icon" viewBox="0 0 50 50">
                        <path d="M25 5 L30 20 L45 25 L30 30 L25 45 L20 30 L5 25 L20 20 Z" fill="none" stroke="#d4af37" stroke-width="1"/>
                        <circle cx="25" cy="25" r="8" fill="none" stroke="#ff6b35" stroke-width="1"/>
                    </svg>
                    <h3 class="tool-title">Daily Spark Oracle</h3>
                    <p class="tool-description">Receive your daily consciousness guidance through sacred randomness and synchronicity.</p>
                    <div class="tool-meta">
                        <span class="tool-tag">Coming Soon</span>
                        <span class="tool-arrow"></span>
                    </div>
                </a>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="cta-section">
            <h2 class="cta-title">Unlock Full Access</h2>
            <p class="cta-text">Join The First Spark community to access all premium tools, exclusive content, and live consciousness sessions.</p>
            <a href="https://thefirstspark.shop" class="cta-button" target="_blank">Explore Membership</a>
        </section>

        <!-- Footer -->
        <footer class="footer">
            <div class="footer-links">
                <a href="https://thefirstspark.shop" class="footer-link" target="_blank">Main Site</a>
                <a href="https://stan.store/thefirstspark" class="footer-link" target="_blank">Shop</a>
                <a href="#" class="footer-link">Contact</a>
            </div>
            <p class="footer-copy">© 2024 The First Spark. All Rights Reserved.</p>
        </footer>
    </div>

    <script>
        // Background animation
        const canvas = document.getElementById('bgCanvas');
        const ctx = canvas.getContext('2d');

        function resize() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        }
        resize();
        window.addEventListener('resize', resize);

        const stars = [];
        for (let i = 0; i < 100; i++) {
            stars.push({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                r: Math.random() * 1.2,
                o: Math.random(),
                s: Math.random() * 0.3 + 0.05
            });
        }

        function animate() {
            ctx.fillStyle = 'rgba(10, 10, 15, 0.15)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);

            stars.forEach(star => {
                star.o += (Math.random() - 0.5) * 0.02;
                star.o = Math.max(0.1, Math.min(1, star.o));

                ctx.beginPath();
                ctx.arc(star.x, star.y, star.r, 0, Math.PI * 2);
                ctx.fillStyle = `rgba(212, 175, 55, ${star.o * 0.4})`;
                ctx.fill();

                star.y += star.s * 0.1;
                if (star.y > canvas.height) {
                    star.y = 0;
                    star.x = Math.random() * canvas.width;
                }
            });

            requestAnimationFrame(animate);
        }
        animate();
    </script>
</body>
</html>
